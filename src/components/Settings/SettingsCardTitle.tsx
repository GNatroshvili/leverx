import React from "react";

const SettingsCardTitle: React.FC = () => (
  <div className="settings-card-title">
    <h1 className="settings-title">ROLES & PERMISSIONS</h1>
  </div>
);

export default SettingsCardTitle;
